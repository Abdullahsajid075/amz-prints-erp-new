# ERP frontend API URL.
# Keep the Apps Script /exec URL until Supabase counts match, then switch.
# Rollback: put the GAS URL back and redeploy.

# Live (current):
# REACT_APP_GAS_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT/exec

# After cutover (Vercel API project, Root Directory = api):
# REACT_APP_GAS_API_URL=https://YOUR-API.vercel.app

# Local API:
REACT_APP_GAS_API_URL=http://localhost:3000
