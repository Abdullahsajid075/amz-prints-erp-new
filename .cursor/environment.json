{
  "name": "AMZ Prints ERP",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/local-backend/start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd api && npm start",
      "description": "Express ERP API on http://localhost:3000 (talks to the local Supabase-compatible backend)."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host",
      "description": "Vite dev server for the React ERP UI on http://localhost:5173."
    }
  ],
  "ports": [
    { "name": "frontend", "port": 5173 },
    { "name": "api", "port": 3000 }
  ]
}
