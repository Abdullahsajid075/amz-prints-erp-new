# Google Apps Script Web App URL (leave empty for mock mode)
REACT_APP_GAS_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Mock mode only (optional — not used when GAS URL is set)
REACT_APP_DEMO_PASSWORD=
REACT_APP_DEMO_TOKEN=
